# 쿠키프렌즈 토벌전 OCR 도구 (Python 3.13) — 실행: py ocr/main.py
# UI만 보려면 PyQt6 만 설치해도 됩니다.
PyQt6>=6.6
mss>=9.0
opencv-python>=4.8
numpy>=1.26

# OCR 엔진 — 전부 PaddleOCR rec 전용(검출 생략, 셀당 ~18ms)
paddleocr>=3.6        # 숫자(딜량·회수, en rec) + 닉네임(korean rec)
paddlepaddle==3.2.0   # ⚠️ 3.3.x는 Python 3.13에서 oneDNN 실행 버그 → 3.2.0 고정
